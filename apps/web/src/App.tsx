// App shell. Real views (load, dashboard, queue, room detail, artifacts, delta)
// arrive in tasks T5/T6.
export function App() {
  return <main>LAUSD Standards/Decision Engine — scaffold (T0)</main>;
}
